name: etegram_flutter_sdk
description: Etegram Flutter SDK for payment initialization and hosted checkout.
version: 0.1.0
homepage: https://github.com/mosesedem/etegram-sdks
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0

flutter:
  uses-material-design: false
